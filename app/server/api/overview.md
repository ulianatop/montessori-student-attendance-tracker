# REST style API using express
## Flow I am trying to follow:
1. Init a database object and pass it to an express router object
2. from the express router for the given data model (student, authorized adult + more todo), register the routes and pass in custom handlers for each function (wip)
#### Makes data and logic seperate / allows dependency injection